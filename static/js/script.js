const enterButton = document.querySelector('.enter-button');

enterButton.addEventListener('click', () => {
  // 在这里添加按钮点击后的行为，例如：
  alert('Welcome to the Neon City!');
  // 或者跳转到其他页面：
  // window.location.href = 'your-next-page.html';
});